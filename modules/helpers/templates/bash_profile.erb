source /opt/boxen/env.sh
if [ -f `brew --prefix`/etc/bash_completion ]; then
    . `brew --prefix`/etc/bash_completion
fi

export CC="gcc-4.2"

export LS_OPTIONS='--color=auto'
export CLICOLOR='Yes'

FRAG="<%= @home %>/.bash_fragments/*.bash"
for dot_file in $FRAG
do
  source $dot_file
done